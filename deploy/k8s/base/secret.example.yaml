# Example only. Do not apply this file with real values committed to git.
# Kubernetes Secret values are base64 encoded, not encrypted. For real
# deployments, create a secret outside git or use your cluster secret manager.
apiVersion: v1
kind: Secret
metadata:
  name: revenue-ops-secret
  namespace: revenue-ops
type: Opaque
stringData:
  N8N_ENCRYPTION_KEY: "REPLACE_ME"
  GEMINI_API_KEY: "REPLACE_ME_OR_EMPTY_FOR_MOCK"
  GHL_API_TOKEN: "REPLACE_ME"
  GHL_SYNC_AUTH_SECRET: "REPLACE_ME"
  AGENT_AUTH_SECRET: "REPLACE_ME"
  SYNC_AUTH_SECRET: "REPLACE_ME"
  MVP_REPLY_WEBHOOK_SECRET: "REPLACE_ME"
  POSTGRES_PASSWORD: "REPLACE_ME"
  DB_POSTGRESDB_PASSWORD: "REPLACE_ME"
  DATABASE_URL: "postgresql://postgres:REPLACE_ME@postgres:5432/revenue_ops"
  BUSINESS_DATABASE_URL: "postgresql://postgres:REPLACE_ME@postgres:5432/revenue_ops"

